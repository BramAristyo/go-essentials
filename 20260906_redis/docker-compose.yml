services:
  redis:
    image: redis:latest
    container_name: redis-local
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    environment:
      - REDIS_ARGS=--requirepass localdev --appendonly yes
    restart: unless-stopped

volumes:
  redis-data:
    driver: local
